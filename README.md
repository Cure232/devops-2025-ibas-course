# Лабораторная работа №3: Docker и CI/CD

## 📌 Описание
В данной лабораторной работе выполнена контейнеризация статического сайта с помощью Docker и интеграция сборки образа в CI/CD пайплайн на GitHub Actions.

## 🐳 Что сделано
- Написан `Dockerfile` на основе образа `nginx:alpine`
- Добавлены `LABEL`, `HEALTHCHECK`, настроены права доступа
- Обновлён `index.html` до версии 3.0 (упоминание Docker)
- Собран Docker-образ локально и запущен с пробросом порта 8181
- Образ опубликован в GitHub Container Registry (`ghcr.io`)
- В CI workflow (`.github/workflows/ci.yml`) добавлены шаги:
  - Линтер Hadolint для проверки Dockerfile (не ломает пайплайн)
  - Сборка и публикация образа в GHCR при push в основную ветку
  - Автоматическое тегирование (ветка, коммит, latest)

## 📁 Структура репозитория (ветка lab3)
```
.
├── .github/workflows/
│ └── ci.yml # CI сборка и публикация Docker образа
├── Dockerfile # инструкции для образа
├── index.html # демо-сайт версии 3.0
├── nginx.conf # конфиг nginx (порт 8181)
└── README.md # этот файл
```

## 🚀 Запуск локально
```bash
# Сборка образа
docker build -t ghcr.io/cure232/lab3-demo:latest .

# Запуск контейнера
docker run -d -p 8181:8181 --name lab3-demo ghcr.io/cure232/lab3-demo:latest

# Проверка
curl http://localhost:8181

# Логин (токен с правами write:packages)
echo "ТОКЕН" | docker login ghcr.io -u cure232 --password-stdin

# Push
docker push ghcr.io/cure232/lab3-demo:latest
```

